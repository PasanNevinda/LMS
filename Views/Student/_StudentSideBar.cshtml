<nav id="sidebar">
    <div class="sidebar-header">
        <a class="sidebar-brand" onclick="showView('dashboard')">
            <i class="bi bi-mortarboard-fill"></i>
            <span>EduPlatform</span>
        </a>
    </div>

    <div class="sidebar-nav">
        <ul class="nav flex-column">
            <li class="nav-item">
                <a asp-action="StudentDashBoard" asp-controller="Student" class="nav-link @(ViewData["CurrentPage"]?.ToString() == "StudentDashboard" ? "active" : "")" >
                    <i class="bi bi-house-door"></i>
                    <span>Dashboard</span>
                </a>
            </li>
            <li class="nav-item">
                <a asp-action="Cart" asp-controller="Student" class="nav-link @(ViewData["CurrentPage"]?.ToString() == "Cart" ? "active" : "")">
                    <i class="bi bi-cart3"></i>
                    <span>Shopping Cart</span>
                </a>
            </li>
            <li class="nav-item">
                <a asp-action="BrowseCourse" asp-controller="Student" class="nav-link @(ViewData["CurrentPage"]?.ToString() == "BrowseCourse" ? "active" : "")">
                    <i class="bi bi-search"></i>
                    <span>Browse Courses</span>
                </a>
            </li>
        </ul>
    </div>
</nav>